# 修复记录

## 修复时间：2026-04-30

### push2 防风控降量改造
- 删除独立 premium_refresh 定时任务，溢价计算合并进 spot（spot 的 `_parse_spot_row` 已含完整 4 级溢价计算）
- 指数数据源顺序改为 Sina → Tencent → Eastmoney（原 Eastmoney 优先，每次 3 个 push2 请求）
- spot 刷新频率 2min → 5min（REFRESH_MINUTES 默认值调整）
- K 线改为增量拉取：有本地缓存时只拉缓存末日之后的数据，失败时返回缓存不丢数据
- 新增 UA 轮换池（8 个浏览器 UA）和 Referer 轮换池（5 个东方财富页面）
- 请求抖动从 0.5-2s 加大到 2-5s
- push2 请求量从 ~320 降至 ~35 req/hr（降 89%）

---

## 修复时间：2026-04-29

### 1. 修复 ETF 规模计算错误
- 移除 f164（流通市值）作为规模来源——流通市值 ≠ 基金总规模
- 规模优先级重排：f117(总市值-股票) → f20(总市值-基金) → f441×f38 → NAV cache
- 新增 f441 单位异常检测：当 iopv×shares 与 f20 偏差 >5× 时记录警告
- 修复案例：511360（显示 664.77 亿，实际 934.26 亿）

### 2. 修复溢价数据精度问题（161226、159509）
- 溢价计算优先级调整：NAV cache + 实时价格计算优先于 _premium_cache 中的旧值
- 修复案例：161226（溢价 25.20% → 25.45%），159509（溢价 19.68% → 16.21%）

### 3. K 线历史截断修复（510050 等早期 ETF）
- 引入常量 `KLINE_HISTORY_START = "20050101"`
- fetch_kline_live/days=None 时使用该常量作为 begin，而不是默认 1200 天（约 2023 年）
- 注：完整历史仍依赖 push2his；腾讯备用约 641 行，Sina 备用 max 1000 行

### 4. push2 WAF 封禁韧性增强
- API_BASE_INTERVAL: 2.5s → 5.0s
- API_MAX_INTERVAL: 15s → 30.0s
- 熔断冷却改为渐进式：每次进入 CIRCUIT_BREAKER_THRESHOLD 后 cooldown 随 streak 递增，上限 1800s（30 分钟循环）
- 请求 jitter 增大：0.02-0.18s → 0.5-2.0s
- 新增腾讯 qt.gtimg 作为指数第三备源（Eastmoney → Sina → Tencent）

---

## 修复时间：2026-04-18
## 修复问题：
1. ETF总数不足2000
2. 溢价字段全为null
3. 指定ETF代码不存在

## 修复内容：
### 1. 修复基金列表不全问题
- 修正`_ensure_all_etfs_in_spot`函数缩进错误
- 扩大东方财富接口基金类型覆盖范围，新增t:18、t:20类型
- 调整分页参数到40页，每页1000条，覆盖所有场内基金
- 添加兜底基金：510300、562510

### 2. 修复溢价不显示问题
- 重构接口溢价处理逻辑，优先使用缓存有效数据
- 异步溢价任务完成后直接更新全局etf_spot
- 定时溢价任务增加全局更新逻辑
- 调整限流策略：每10条请求暂停2秒，降低反爬风险

### 3. 修复指定ETF缺失问题
- 5只指定ETF全部添加到兜底列表，确保存在