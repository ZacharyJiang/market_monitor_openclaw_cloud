# 2026-04-19 监控任务执行记录

## 00:34 执行 - 24h看门狗

### 网站可访问性
- ✅ 网站 API 可正常访问（通过 Python requests）
- ❌ curl 访问超时（本地VPN/代理拦截 198.18.0.44）
- ✅ Health API 返回 200

### 数据完整性检查
| 指标 | 值 | 状态 |
|------|-----|------|
| ETF总数 | 1886 | ⚠️ 偏低(应>2000) |
| 价格覆盖率 | 94.1% (1774/1886) | ⚠️ |
| 溢价率覆盖率 | 0% (0/1886) | ❌ 严重异常 |
| NAV覆盖率 | 0% (0/1886) | ❌ 严重异常 |
| 费率覆盖率 | 73.0% (1377/1886) | ⚠️ |
| 规模覆盖率 | 53.4% (1007/1886) | ⚠️ |

### 问题诊断
1. **溢价率100%缺失**: `_parse_spot_row` 未解析列表API的 f183/f184 字段；`_fetch_premium_batch_sync` 非交易时间 f184=0 时无法获取溢价
2. **价格部分缺失**: `refresh_spot` 合并逻辑直接覆盖旧数据，非交易时间获取的空数据覆盖了有效价格
3. **ETF总数偏低**: 列表API fs参数缺少 t:18(商品)/t:20(债券) 类型

### 修复措施 (commit 7856640)
1. `_parse_spot_row`: 新增解析 f183(净值)/f184(溢价率)，同步更新 _premium_cache
2. `refresh_spot`: 智能合并新旧数据，新数据为0/null时保留旧数据有效值
3. `get_etf_data`: 移除 premium!=0 过滤，0也是有效溢价率
4. `_fetch_premium_batch_sync`: 3级溢价获取策略(官方f184→手动f43/f183计算→spot价格+API净值)
5. `_fetch_spot_from_endpoint`: fs参数增加 t:18/t:20 类型覆盖

### 部署状态
- 代码已推送到 GitHub main 分支
- Webhook 已触发，auto-update.sh 在后台执行
- ⚠️ 截至检查时间，新代码尚未生效(updated 时间戳未变)
- 可能原因: Docker 重建时间较长，或服务器上仓库路径不一致
- 建议下次手动SSH到服务器检查部署日志

---

## 01:59 执行 - 24h看门狗（第2次）

### 网站可访问性
- ✅ 前端页面正常 (HTTP 200, 28755 bytes)
- ✅ API端点正常 (/api/etf-data, /api/health, /api/diag)
- ✅ 指数数据正常 (上证4051.43, 深证14885.42, 沪深3004728.67)

### 数据完整性检查
| 指标 | 值 | 状态 |
|------|-----|------|
| ETF总数 | 1886 | ⚠️ 偏低 |
| 溢价率覆盖率 | 0% (0/1886) | ❌ 严重异常 |
| NAV覆盖率 | 0% (0/1886) | ❌ 严重异常 |
| 费率覆盖率 | 73.0% (1377/1886) | ⚠️ |
| 价格覆盖率 | 94.1% (1774/1886) | ⚠️ |
| K线覆盖率 | 94.6% (1784/1886) | ✅ |

### 根因分析
**溢价率和净值100%缺失的根本原因**：东方财富单条股票API (`push2.eastmoney.com/api/qt/stock/get`) 缺少 `fltt=2` 参数。

- 列表API使用了 `fltt=2`，返回实际值（如 f43=4.739）
- 但单条API（`_fetch_premium_batch_sync` 和 `_fetch_premium_from_eastmoney`）**没有** `fltt=2`，返回放大1000倍的整数（如 f43=4739）
- 代码只对 f43(price) 做了放大1000倍的安全处理（>100则除以1000）
- **f183(nav) 完全遗漏了这个处理！** nav_raw=4750 直接当作4.75使用
- 导致溢价率计算结果异常（如 (4.739-4750)/4750 ≈ -99.9%），被 abs(premium)<30 过滤掉
- 最终 _premium_cache 始终为空，所有ETF的premium和nav字段都是null

### 修复措施 (commit 1461e0f)
1. `_fetch_premium_batch_sync`: 添加 `fltt=2` 参数，对 f183(nav) 增加放大1000倍的安全判断
2. `_fetch_premium_from_eastmoney`: 同上，添加 `fltt=2` 参数和 f43/f183 安全处理
3. `check_and_fill_missing_data` 价格补全: 添加 `fltt=2` 参数，f43/f170 安全判断
4. 方法3中使用处理后的 `nav` 变量替代原始 `nav_raw`

### 部署状态
- ❌ 代码已推送 GitHub (commit 1461e0f)，但自动部署未成功
- Webhook 触发成功（宿主机 webhook-server.py 返回 200），但 auto-update.sh 执行后 Docker 容器未重启
- 更新时间仍然停留在 2026-04-19 00:48:18
- 可能原因: OpenClaw 平台的 Docker 管理机制与 auto-update.sh 不兼容
- **需要用户手动SSH到服务器部署，或通过 OpenClaw 平台重启容器**
